/** @type {import('prettier').Config} */
const config = {
    tabWidth: 4,
    useTabs: false,
    semi: false,
    bracketSpacing: true,
    singleQuote: true,
    trailingComma: 'es5',
}

export default config
